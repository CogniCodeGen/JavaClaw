#!/bin/sh
set -eu
APP_ROOT=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
JAVACLAW_PROGRAM_DIR=${JAVACLAW_PROGRAM_DIR:-$APP_ROOT}
export JAVACLAW_PROGRAM_DIR
exec "$APP_ROOT/runtime/bin/java" -cp "$APP_ROOT/lib/*" com.javaclaw.launcher.JavaClawLauncher "$@"
