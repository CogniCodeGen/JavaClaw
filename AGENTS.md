# JavaClaw 开发约定

本文件适用于整个仓库。每次新增、修改或重构代码，都必须遵守下列约定，包括示例、测试和生成后纳入维护的源码。

## 格式与可读性

- 遵守 `docs/coding-style.md` 和 `.editorconfig`；Java 使用 UTF-8、LF、4 空格缩进和 120 列换行目标。
- 使用明确命名、完整控制块、方法间留白和显式 import；禁止通配符 import、一行多条语句和压缩非空方法体。
- 统一使用项目锁定的 Spotless 格式化器，不以个人 IDE 设置覆盖项目规范。
- 不为追求形式上的“优雅”改变初始化顺序、事务边界、线程模型、权限策略或公共 API。

## 注释与契约

- 新增或重写的注释使用中文，保留类名、协议名和专业术语的英文；版权及官方规范原文不翻译。
- 公共类型、公共/受保护方法和构造器必须有准确的 Javadoc，说明用途、约束、返回语义及重要副作用。
- Record 在类型文档中说明组件含义、单位和可空性；自动 accessor 不重复注释。实现已有契约的 `@Override` 可继承文档，特殊行为须补充说明。
- 并发、事务、取消、背压、预算、缓存失效、权限撤销、FFM 资源所有权和 OAuth 校验必须说明关键不变量与设计原因。
- 修改行为时同步更新相关注释，删除过时说明。禁止用重复方法名、机械翻译或注释行数充当文档质量。
- 测试名称表达场景；复杂夹具和安全断言说明意图，不要求每个测试方法都写 Javadoc。

## 交付前检查

1. 阅读受影响代码与契约，保留用户已有未提交修改。
2. 执行 `mvn spotless:apply`，检查实际 diff；不得借格式化修改官方 Schema、golden fixture 或历史 migration。
3. 执行 `mvn spotless:check checkstyle:check` 和相关测试；大范围改动运行完整 `verify` 门禁。
4. 交付说明中列出验证结果、未验证的平台与真实限制；不通过关闭规则或全模块 suppression 掩盖问题。

构建默认只检查、不改写源码。不要自动安装 Git hook、修改个人全局配置或提交工作树。

## 产品与提示词边界

- 技术架构重构必须沿用 `509f197` 的 JavaFX 布局、CSS 令牌与交互语言；新增控件复用该设计体系，不重新设计产品外观。
- Desktop 仅通过 SDK 使用服务端；旧 UI 源码只作表现层参考，不恢复旧 Runtime、Spring Context 或数据读取链。
- 提示词不能代替工具、安全策略或业务状态机。外部资料与摘要不是系统指令，能力必须来自本 Turn 的实际快照。
- 内置提示词随代码审阅、版本化和测试；用户 Profile、已确认项目指令和 Skill 内容不得静默覆盖。
- 不调用付费模型作为默认测试，不宣称未经真实对照评估的效果提升；未完成能力与未验证平台必须如实记录。
