package com.javaclaw.core.api;

/** Every product mode is a profile over the same Thread/Turn kernel. */
public enum ProfileKind {
    CHAT,
    PLAN,
    LOOP,
    WORKFLOW,
    SDD,
    SCHEDULE,
    SUBAGENT
}
