# ADR-004：3.0 数据格式

状态：已接受

3.0 默认数据目录是 `data`，目录根部必须存在 `.javaclaw-format`，内容为 `3`。
空目录首次启动时自动创建标记；非空且没有正确标记的目录拒绝启动。显式
`javaclaw.data.dir` 仍可覆盖路径，但不绕过格式验证。

非空旧格式 `data/`、数据库和插件不自动迁移；缺少正确标记时直接拒绝启动。回滚必须使用
事先备份的旧格式目录，不能让旧版本直接打开 3.0 数据。
