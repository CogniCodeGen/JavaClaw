# ADR-003：执行与取消模型

状态：已接受

阻塞 I/O 使用受 256 并发配额保护的虚拟线程；CPU 工作使用有界平台线程池；
浏览器和进程分别使用 4 与 8 的全局配额。单线程调度器只触发任务，任务正文始终
交给对应执行器。

所有后台工作通过 `ManagedTaskExecutor` 登记并返回 `TaskHandle`。关闭作用域时先
拒绝新任务，再请求取消，最多等待五秒后释放资源。迟到的 UI 结果必须被丢弃。
