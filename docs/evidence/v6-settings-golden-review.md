# v6 设置中心导航文案视觉记录

本次更新 54 张 macOS aarch64 设置中心壳/外观页参考图与清单，接纳 v6 要求内的导航文案变化：原“智能体方案”
入口改为“Agent Studio”，并更新角色职责说明。参考图来自真实 JavaFX Golden 运行产物；未修改布局、CSS、截图渲染器
或逐字节比较门禁。2026-09-07 的完整 `clean verify` 已通过独立 JVM 的 54 图 Golden，命令与日志哈希见
[本机构建证据](v6-build-validation.md)。

对照范围为九主题、三密度、两个窗口尺寸和 100% 字号，共 54 图。每张图的变动均局限于左侧 Agent 标题与描述；
三种差异边界分别为 `[31,269,181,312]`、`[33,284,183,327]` 和 `[36,299,162,342]`，框外每一像素完全一致。
另查看了 `graphite/compact/standard` 与 `emerald/spacious/minimum` 的前后图，确认差异与要求内文案更新一致。

[逐图差异记录](v6-settings-golden-diff.json)保留 Git 对照提交、平台、54 张图的更新前后 SHA-256、差异边界与
框外像素相同的断言。原图可从记录中的 Git 提交恢复；本次参考更新有可追溯依据，不用于消除未知或未审阅差异。

此记录只证明当前设置中心壳/外观页及导航文案的差异范围。Agent Studio 正文、新执行选择器、其他页面状态、其余
字号和 Linux/Windows 仍需补充视觉证据。普通 `verify` 继续禁止自动改写参考图，并按现有门禁比较当前输出。
